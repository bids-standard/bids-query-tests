[project]
name = "bids-query-tests"
version = "0.1.0"
description = "Test fixtures for implementations of BIDS querying tools"
readme = "README.md"
authors = [
    { name = "Christopher J. Markiewicz" },
    { name = "Alejandro de la Vega" },
]
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.1.1",
]

[build-system]
requires = ["uv_build>=0.10.2,<0.11.0"]
build-backend = "uv_build"
